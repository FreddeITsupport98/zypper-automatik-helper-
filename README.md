# zypper-automatik-helper-
Zypper Auto-Downloader  A systemd timer and "bulletproof" notification script that automatically downloads openSUSE Tumbleweed updates in the background and sends a desktop notification—complete with the new snapshot version and package count—when they're downloaded and ready to install.
